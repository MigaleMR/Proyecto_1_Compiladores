! Programa simple para probar el comando MATCH sin otherwise
let var x : Integer;
    var resultado : Integer
in
begin
  getint(var x);
  
  match x of
    case 1: resultado := 10
    case 2: resultado := 20  
    case 3: resultado := 30
  end;
  
  putint(resultado)
end
